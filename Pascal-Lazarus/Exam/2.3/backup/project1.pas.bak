program z1;
uses crt;
var a:string;i,n:byte;
  begin
    readln(a);
    n:=length(a);
    for i:=1 to n do
      if (ord(a[i])>=65) and (ord(a[i])<=90) then a[i]:=chr(ord(a[i])+32);
    writeln(a);
    readln;
    end.
