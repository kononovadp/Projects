Ћюбой переменной, объ€вленной с помощью директив описани€ простых типов данных, ассемблер присваивает три атрибута:

    —егмент (seg) Ч адрес начала сегмента, содержащего переменную;
    —мещение (offset) в байтах от начала сегмента с переменной;
    “ип (type) Ч определ€ет количество пам€ти, выдел€емой переменной в соответствии с директивой объ€влени€ переменной. 